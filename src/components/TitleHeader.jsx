import React from 'react';

const TitleHeader = ({ title }) => {
  return (
    <h1 className="text-4xl font-bold underline text-blue-800 mb-5">{title}</h1>
  );
};

export default TitleHeader;
